# TANTANGO

英単語学習アプリです。

このアプリは、英単語データをアプリ内のファイルから読み込んで表示します。
Gemini / OpenAI などのAI APIキーは使いません。

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
